[package]
name = "collections-gpui-standalone"
version = "1.11.3"
license = "Apache-2.0"
description = "Standard collection types used by Zed and GPUI"
edition = "2024"
repository = "https://github.com/zed-industries/zed"

[lib]
path = "src/collections.rs"
doctest = false
name = "collections"

[features]
test-support = []

[dependencies]
gpui_util = {package = "gpui_util-gpui-standalone", version = "1.11.3"}
indexmap = {version = "^2.7.0", features = ["serde"]}
rustc-hash = "^2.1.0"
